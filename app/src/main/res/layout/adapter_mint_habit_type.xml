<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="@dimen/sc_mm_168"
    android:layout_height="wrap_content"
    android:orientation="vertical">
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <ImageView
            android:id="@+id/iv_habit_type"
            android:layout_width="match_parent"
            android:layout_height="@dimen/sc_mm_168"
            android:scaleType="centerCrop"
            />
        <com.amkj.dmsh.views.RoundImageView
            android:id="@+id/iv_round_habit_type_fg"
            android:layout_width="match_parent"
            android:layout_height="@dimen/sc_mm_168"
            android:scaleType="fitCenter"
            android:visibility="gone"
            android:src="@drawable/habit_check_bg"

            app:radius="@dimen/dimen_4dp"
            app:type="round"/>
    </FrameLayout>

    <TextView
        android:id="@+id/tv_habit_type_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:text="母婴"
        android:layout_marginTop="@dimen/sc_mm_12"
        android:textColor="@color/text_login_gray_s"
        android:textSize="@dimen/sc_mm_26" />
</LinearLayout>